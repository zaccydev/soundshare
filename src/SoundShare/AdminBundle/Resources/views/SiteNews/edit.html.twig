{% form_theme edit_form 'SoundShareCommunityBundle:Form:fields.html.twig' %}
{%extends "SoundShareCommunityBundle::layout.html.twig" %}

{% block content %}
    <h1>SiteNews edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_sitenews') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
